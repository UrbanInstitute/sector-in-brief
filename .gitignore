# R session state
.Rproj.user/
.Rhistory
.RData
.RDataTmp
.Ruserdata

# renv library (lock is tracked; library is not)
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/staging/

# Build / deploy caches
.Rcheck/
rsconnect/

# Load testing artefacts
shinycannon-*.jar
run*/
run*.html
recording.log
*.log
bench/

# Local probe / smoke scripts for the download API (scratch, never committed)
dev/

# Data files synced from S3 at app startup (per ADR 0011)
data/*.parquet
data/_manifest.json
# Data dictionary CSV is regenerated from data/data_dictionary.parquet on boot
www/data_dictionary.csv
R/sysdata.rda

# Generated at deploy time from CI secrets (AWS invoke creds for the download
# form); bundled to shinyapps.io but never committed (see deploy-*.yml)
.Renviron
